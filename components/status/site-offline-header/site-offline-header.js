const OfflineHeader = () => {

    return (
        <div className="admin-content-box">

            <div>
                <h4>You are logged in but site is offline mode!</h4>
            </div>

        </div>
    );
};

export default OfflineHeader;
